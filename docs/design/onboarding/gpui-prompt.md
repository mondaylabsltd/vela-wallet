创建一个新分支，用 GPUI 在 `app-desktop/vela-wallet` 中重写 Vela Wallet 的桌面端界面。

目标与范围：
- 本次首发仅实现 Onboarding 页面，不涉及移动端适配。
- 支持浅色与深色模式，并确保两种模式下的颜色、对比度和交互状态一致。
- 设计稿参考：`/Volumes/data/production/vela-wallet/design/onboarding`
- GPUI 的实现方式可参考：`/Volumes/data/production/gpui-demo`

实施要求：
1. 接入并遵循项目中的 `docs/design-system.md`，将颜色、字体、间距、圆角、阴影及组件状态抽象为统一设计令牌或可复用组件。
2. 接入 `rust/crates/vela-core` 中现有的 i18n/l10n 能力。页面上的所有用户可见文本均不得硬编码，应使用本地化资源。
3. 使用 GPUI 实现桌面端 Onboarding 页面，尽可能还原设计稿的布局、视觉层级和交互反馈。
4. 组件与业务逻辑应高内聚、低耦合：
   - 可复用的视觉组件、主题逻辑、本地化逻辑和页面业务逻辑分别归位；
   - 避免重复实现颜色、样式、文案或交互逻辑；
   - 不要将同一功能分散在多个无关文件中；
   - 优先使用清晰的模块边界和单一职责设计，便于后续扩展与维护。
5. 保持改动范围聚焦，不修改与 Onboarding 或必要基础设施无关的功能。

完成后请提供：
- 新分支名称；
- 改动文件与主要设计说明；
- 浅色/深色模式及本地化接入的验证结果；
- 可复用组件或设计令牌的说明；
- 已知限制或后续建议。