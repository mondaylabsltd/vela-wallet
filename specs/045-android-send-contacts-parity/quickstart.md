# Quickstart — verifying 045 on the device

Parallel space on the Xiaomi; Gnosis; `adb push` for files.

1. **Split** (SC-001): 转账 → XDAI → the form's "add recipient" → founder
   `0x7687…` 0.001 and the Safe `0x88cC…` 0.001 → 继续 → confirm shows
   `2 recipients`, total 0.002 → slide → one op hash; the feed shows two
   rows.
2. **Sweep pick** (SC-002): 转账 → 发送多个代币 → tick XDAI → the notice
   names Gnosis, other chains' rows dim; "select all valuable" ticks only
   visible valuable rows; clear → tick again pins again.
3. **Batch** (SC-003): `adb push two-rows.csv /sdcard/Download/` → split
   form → 导入 → the sheet → 从文件导入 → DocumentsUI → Downloads →
   two-rows.csv → preview 2 rows → 应用 → two split rows → send.
4. **Form** (SC-005): 通讯录 → 添加联系人 → name + address → 保存 → in the
   list; open → 编辑 → rename → save; the star → favourites; force-stop →
   still there.
5. **Book** (SC-006): ⋯ → 导出 → JSON → share sheet → save to Files; ⋯ →
   导入 → pick it → report `0 added, N already existed`.
6. **Inspection** (SC-007): open the founder's contact → `Wallet` tag and
   first-time-or-not; open the USDC contract saved as a contact →
   `Contract`.
