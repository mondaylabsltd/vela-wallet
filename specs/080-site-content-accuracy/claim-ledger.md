# Claim ledger — 080

Every fact that appears on more than one public surface, with **one** value and
its canonical English and Chinese wording. Rewrites and translations follow this
table; changing a value means changing every surface in the same commit.
Evidence: [research.md](research.md) section in brackets.

| id | Canonical English | 中文（定稿） | Evidence | Check |
|---|---|---|---|---|
| C-net-1 | Vela has **24 built-in networks** (all mainnets): Ethereum, BNB Chain, Polygon, Arbitrum, Optimism, Base, Avalanche, Gnosis, Unichain, Tempo, Monad, World Chain, Arc, X Layer, Stable, Soneium, MegaETH, Robinhood Chain, Mantle, Kaia, Celo, Ink, Plume, XRPL EVM — and you can add others that meet its requirements. | 内置 **24 条网络**（均为主网）……另可添加满足要求的其他 EVM 网络。 | [§1] | no "12 networks", "twelve", "8 networks" left |
| C-addr-1 | Your address is derived from **all the keys you create the wallet with**, and is the same on every network. | 地址由**创建钱包时的全部钥匙**推导而来，在每条网络上都相同。 | [§1] | no "derived from your passkey's public key" (singular) |
| C-keys-1 | A wallet can have **up to seven keys**, chosen **when you create it**. **Any one of them can sign on its own** (1-of-n). Keys cannot be added, removed or replaced later. | 一个钱包最多可以有 **7 把钥匙**，在**创建时**选定；**任意一把都能单独签名**（1-of-n）。之后不能再增加、删除或替换。 | [§1] | every page that explains keys says all three |
| C-keys-2 | Kinds of key: **a passkey on this device** (synced by iCloud Keychain, Google Password Manager or your password manager, if you allow it), **another phone** you reach by scanning a QR code, or a **hardware security key** (USB; NFC where the browser supports it). | 钥匙的种类：**本设备上的通行密钥**（若你允许，会由 iCloud 钥匙串、Google 密码管理器或你的密码管理器同步）、扫码连接的**另一部手机**、或**硬件安全密钥**（USB；浏览器支持时也可用 NFC）。 | [§1] | no page says keys are only iCloud/Google or only "face or fingerprint" |
| C-auth-1 | Every signature needs the key's own check — Face ID, fingerprint, device PIN, or a touch and PIN on a security key. | 每次签名都要经过钥匙自身的验证——面容、指纹、设备密码，或安全密钥上的触碰与 PIN。 | [§1] | no "your face or fingerprint" as the only way |
| C-acct-1 | Your wallet is an **unmodified Safe v1.4.1**, run through **ERC-4337 (EntryPoint v0.7)** with Safe's **4337 module v0.3.0**, and verified by Safe's **passkey module v0.2.1** (the shared signer for the first key, a signer created by Safe's factory for each additional key). **No contract in the funds path was written by Vela.** | 你的钱包是**未经修改的 Safe v1.4.1**，通过 **ERC-4337（EntryPoint v0.7）**和 Safe 的 **4337 模块 v0.3.0** 运行，由 Safe 的**通行密钥模块 v0.2.1** 验签（第一把钥匙用共享签名器，其余每把各用 Safe 工厂创建的签名器）。**资金路径上没有一个合约是 Vela 写的。** | [§1] | contract table lists factory + singleton; fallback handler = 4337 module |
| C-p256-1 | On-chain passkey verification uses the **P-256 precompile at `0x100` — EIP-7951 on Ethereum (Fusaka, December 2025), RIP-7212 on rollups, same interface**. There is no fallback verifier, and `0x100` is part of every address — so a chain without it cannot run Vela. | 链上验签使用 `0x100` 处的 **P-256 预编译——以太坊上叫 EIP-7951（Fusaka 升级，2025 年 12 月起），rollup 上叫 RIP-7212，接口相同**。没有备用验证器；`0x100` 是每个地址推导的一部分，所以不支持它的链无法运行 Vela。 | [§1]; EIP-7951 (Final, Fusaka) | roadmap no longer promises a "path for chains without P-256"; name it "EIP-7951 / RIP-7212", not "formerly RIP-7212" — rollups still ship RIP-7212 |
| C-deploy-1 | You can receive before the wallet exists on a chain; it deploys itself in your first transaction there, and that transaction's fee covers the deployment. | 在某条链上，钱包合约还不存在时就能收款；你在那条链上的第一笔交易会顺带部署它，部署费用包含在这笔交易的手续费里。 | [§1] | — |
| C-fee-1 | Each transaction's fee is paid from your wallet, in the network's coin or a supported USD stablecoin (pathUSD on Tempo). It is **three times the gas the wallet reserves for the transaction, priced at the higher of the wallet's own gas-price reading and the relay's price for your chosen speed**, with a $0.01 minimum. The padding and the price headroom put it above the transaction's actual on-chain cost, more for the first transaction on a network; the relay keeps the difference. **The exact amount is shown before you sign** and is part of what you sign. **It goes to the relay the wallet is set to** — Vela's by default; you can switch to another vela-relay deployment or run your own. | 每笔交易的手续费从你的钱包里扣，用网络原生币或支持的美元稳定币（Tempo 上用 pathUSD）。金额是**钱包为这笔交易预留的 gas 的三倍，按钱包自己读到的 gas 价格与中继对你所选速度的报价中较高者计价**，最低 0.01 美元。预留量和价格都留有余量，所以手续费高于这笔交易的实际链上成本，某条网络上的第一笔交易更高，差额归中继。**确切金额在你签名前显示**，并且写在你签名的内容里。**手续费付给钱包所设的中继**——默认是 Vela 的；你可以换成其他 vela-relay 部署，或自己运行一个。 | [§2] | no "≈2×", "3× the on-chain cost", "no hidden markup"; don't lead with a multiple of the on-chain cost (founder, 2026-09-22) — lead with: shown before you sign, paid to the relay, relay replaceable |
| C-fee-2 | There is **no per-network deposit or gas account**. If a relay's own gas tank on a network runs dry, Vela tells you; on Vela-run networks you can report it or, optionally, top the tank up yourself — that top-up is non-refundable and does not pay for your transaction. | **没有按网络收取的押金或“gas 账户”。** 如果某条网络上中继自己的 gas 用完了，Vela 会告诉你；在 Vela 运营的网络上你可以报告，也可以选择自己补充——这笔补充不退还，也不抵你的手续费。 | [§2] | no "activation", "gas account", "non-refundable deposit to activate" |
| C-fee-3 | No paymaster: nobody sponsors your gas, and nobody can gate your transactions with a sponsorship policy. You can choose a speed (default: fast). | 没有 paymaster：没有人替你付 gas，也没有人能借赞助策略卡住你的交易。你可以选择速度（默认：快）。 | [§2] | no "there is no speed picker" |
| C-relay-1 | The relay receives an already-signed operation. It can delay or refuse it, but cannot change it. You can point the wallet at another **vela-relay** instance; generic ERC-4337 bundlers don't implement Vela's fee quote. | 中继收到的是已经签好名的操作。它可以延迟或拒绝，但改不了内容。你可以把钱包指向另一个 **vela-relay** 实例；通用 ERC-4337 打包器不支持 Vela 的报价方法。 | [§2, §3] | "switch relayers" → "point the wallet at another relay" |
| C-audit-1 | Safe's contracts, the 4337 module, the passkey module and EntryPoint v0.7 have published third-party audits. **Vela's own code — the apps, the backend services and the registry contract — has not had a third-party audit, and none is scheduled.** | Safe 合约、4337 模块、通行密钥模块和 EntryPoint v0.7 都有公开的第三方审计报告。**Vela 自己的代码——各个 App、后端服务和注册表合约——没有经过第三方审计，目前也没有排期。** | [§1], A02 FR-2 | landing trade-off #3 says the second sentence too |
| C-rp-1 | Passkeys are bound to the domain they were created for — Vela's are bound to `getvela.app`. A copy of the web wallet on another domain creates a **different** wallet. To keep signing for an existing wallet without getvela.app, use the Vela browser extension, or a self-built app with a phone (QR) or security key. (The signing page is not a way in on its own.) Browsers offer the passkeys only to getvela.app and its subdomains; outside the browser, extensions with permission and programs using CTAP can request them. | 通行密钥绑定在创建时所用的域名上——Vela 的绑定在 `getvela.app`。把网页钱包部署到别的域名，得到的是**另一个**钱包。想在没有 getvela.app 的情况下继续给已有钱包签名，可以用 Vela 浏览器扩展，或自己编译的 App 配合手机扫码或安全密钥。（签名页本身不是一条独立的路。）浏览器只把这些通行密钥提供给 getvela.app 及其子域名；在浏览器之外，获得权限的扩展和使用 CTAP 的程序也能请求它们。 | [§3a] | whitepaper, FAQ, account-contract agree |
| C-safeui-1 | Any Safe tool can **read** your account and build transactions for it; **signing** needs software that can ask your key for a `getvela.app` signature (above). | 任何 Safe 工具都能**读取**你的账户并为它构造交易；但**签名**需要能向你的钥匙请求 `getvela.app` 签名的软件（见上）。 | [§3a] | no "any Safe-compatible interface can drive it" |
| C-sync-1 | Account lists aren't synced between devices. On a new device, sign in with any of your keys and the wallet is rebuilt from the public registry. | 账户列表不会在设备之间同步。在新设备上用任意一把钥匙登录，钱包会根据公开注册表重建。 | [§1, §5] | no "accounts follow you through platform backup" |
| C-reg-1 | Creating a wallet publishes to a public registry contract on Gnosis: each key's public key, credential ID, authenticator model and flags, your wallet name and key labels, the wallet address, and the signed registration data. It is permanent and readable by anyone. | 创建钱包时会把以下内容写进 Gnosis 上的公开注册表合约：每把钥匙的公钥、凭证 ID、认证器型号与标志位，钱包名称和钥匙名称，钱包地址，以及签名后的注册数据。永久保存，任何人可读。 | [§4] | privacy, terms, create-wallet agree |
| C-dapp-1 | dApps connect through an injected provider (EIP-1193 / EIP-6963) in the **Vela browser extension** and in the browser built into the **desktop (macOS, Windows), iOS and Android apps**. The hosted web wallet does not connect to dApps; there is no WalletConnect. | dApp 通过注入的 provider（EIP-1193 / EIP-6963）连接：**Vela 浏览器扩展**，以及**桌面版（macOS、Windows）、iOS 和 Android App** 内置的浏览器。网页版钱包不连接 dApp；不支持 WalletConnect。 | [§5] | FAQ no longer implies Linux desktop / web |
| C-signpage-1 | The independent signing page is built and tested but **not published**; you load it from the source as a Chrome extension or serve it yourself. | 独立签名页已完成并测试过，但**尚未发布**；需要从源码以 Chrome 扩展方式加载，或自己部署。 | [§5] | FAQ, compare row, bybit page agree |
| C-plat-1 | Web: free, live. Chrome extension: free, download and load unpacked (not yet on the Chrome Web Store). Desktop (Windows, macOS, Linux): free download. iOS and Android: coming to the App Store and Google Play as a one-time purchase; not yet available; free to build from source. | 网页版：免费，已上线。Chrome 扩展：免费，下载后手动加载（尚未上架 Chrome 应用店）。桌面版（Windows、macOS、Linux）：免费下载。iOS 和 Android：将以一次性买断在 App Store 和 Google Play 上架，目前还没有；从源码编译免费。 | [§5] | no "nothing to download / no app store", no "mobile coming soon" as the only other option |
| C-clear-1 | Transactions are decoded before you sign: built-in ERC-7730 descriptors for common contracts, descriptors fetched from Vela's chain-data server, token-standard shapes, then a public selector database (labelled best effort); anything else gets an explicit blind-signing warning. Fetched descriptors are not cryptographically authenticated. | 签名前会解码交易：先用内置的常见合约 ERC-7730 描述文件，再用从 Vela 链数据服务获取的描述文件、代币标准形状，然后查询公共函数选择器数据库（标注为“尽力解析”）；都不行就明确提示盲签。获取来的描述文件没有密码学认证。 | [§1, protocol Q11] | "verified" not described as cryptographic |
| C-approve-1 | An on-chain approval at the "unlimited" level (≥ 2^200; ≥ 2^152 for Permit2) is shown in red. On a single approval you can replace it with a specific amount, your balance (when Vela can read it) or a revoke (not offered on `increaseAllowance`, where it would only add zero); unless you do, it is sent exactly as the dApp built it — Permit2 is designed around a standing approval, and a smart-account batch spends it in the same transaction, so a cap below that spend fails the whole batch. Inside a batch, each unlimited approval gets its own cap (an amount or a revoke) in every app. The submission check reads the raw calldata and refuses an unlimited on-chain approval unless the approval screen reported that you kept it. Signed permits (EIP-2612, Permit2 signatures) can't be capped — the dApp submits its own copy — so they are signed as asked or rejected: an unlimited one is shown in red, a limited one with a caution. A large finite approval is shown with a caution. A request to grant `setApprovalForAll` for a whole NFT collection can't be approved in the apps yet. | 达到“无限”级别（≥ 2^200；Permit2 为 ≥ 2^152）的链上授权会以红色显示。单笔授权可以改成具体金额、你的余额（Vela 能读到时）或撤销（`increaseAllowance` 不提供撤销，因为那只会增加 0）；不改的话，就按 dApp 构造的原样发出——Permit2 的设计依赖一笔常驻授权，智能账户的批量交易会在同一笔里花掉它，上限低于这笔花费就会让整个批量失败。批量交易里的每一笔无限授权，在所有 App 里都能单独设上限（具体金额或撤销）。提交前的检查直接读取原始 calldata，除非授权界面报告你选择了保留，否则拒绝发出无限链上授权。签名式授权（EIP-2612、Permit2 签名）无法设上限——dApp 提交的是它自己那份——所以只能原样签或拒绝：无限的以红色显示，有限的显示警示。金额很大但有限的授权会显示警示。对整个 NFT 合集的 `setApprovalForAll` 授权请求，App 里目前还不能批准。 | [protocol Q11]; founder ruling 2026-09-26 (`approval_guard.rs` module doc) | never "unlimited approvals are blocked / can't be submitted" (true until 2026-09-26, false since); never "one tap" (Custom means typing) |
| C-lic-1 | Everything is MIT-licensed: the wallet (apps and core), the relay, the public-key index, the exchange-rate service and the chain-data directory (the last maintained on the founder's account). | 全部采用 MIT 许可：钱包（App 和核心）、中继、公钥索引、汇率服务和链数据目录（最后一项放在创始人个人账号下）。 | [§3]; p256-index#7 | p256-index MIT since p256-index#7 (merged 2026-09-22) |
| C-selfcall-1 | A dApp can request a call from your Safe to itself (`enableModule`, `addOwnerWithThreshold`, `setFallbackHandler`, `setGuard`); any one, signed once, hands over the account. Vela decodes these calls but does not block them yet. Reject any request whose target is your own address. | dApp 可以请求从你的 Safe 调用它自己（`enableModule`、`addOwnerWithThreshold`、`setFallbackHandler`、`setGuard`）；其中任何一个签一次就会交出账户。Vela 会解码但目前不会拦截。请拒绝任何目标是你自己地址的请求。 | review; `approval_guard.rs`, no guard found | whitepaper, bybit, audits agree |
| C-compromise-1 | A key can't be removed. If one may be in someone else's hands, move your funds to a new wallet: the old address stays spendable by that key on every network, including funds sent later. | 钥匙无法删除。如果某把钥匙可能落入他人之手，请把资金转到新钱包：旧地址在每条网络上都仍可被那把钥匙动用，包括以后转入的资金。 | [§1] | signers, recovery, whitepaper, landing FAQ |
| C-custody-1 | Vela holds no key and no role on your Safe, so it can't move or freeze funds by itself; it does write and serve the software that asks your keys to sign. | Vela 不持有钥匙，在你的 Safe 上没有角色，所以自己无法动用或冻结资金；但请你的钥匙签名的软件是它编写并提供的。 | review | no bare "Vela cannot move your funds" |
| C-selfhost-2 | Every service Vela runs is open source and can be run by someone else, and each can point at the others: the relay reads chain data from the directory set in `VELA_RELAY_CHAIN_DIRECTORY_URL`. | Vela 运行的每一项服务都是开源的、别人也能运行，并且可以互相指向：中继从 `VELA_RELAY_CHAIN_DIRECTORY_URL` 设定的目录读取链数据。 | [§3]; vela-relay#12 | needs vela-relay v0.9.6 or later (released 2026-09-22); older builds read Vela's copy |
| C-access-1 | **Access to your wallet doesn't depend on Vela staying online.** An existing wallet signs through the Vela browser extension or an app you build (phone by QR or security key), and every service it needs can be run by you. The passkeys' domain, `getvela.app`, cannot move: a web copy on another domain is a different wallet. | **即使 Vela 停止服务，你仍然可以访问自己的钱包。**已有的钱包可以通过 Vela 浏览器扩展或你自己编译的 App（扫码手机或安全密钥）签名，它需要的每一项服务都可以由你自己运行。通行密钥所属的域名 `getvela.app` 无法迁移：放到别的域名上的网页版是另一个钱包。 | [§3]; self-hosting `#if-getvela-app-disappears` | landing fact #4 (059 string, restored 2026-09-22) always links to the section that lists the ways in |
| C-sign-1 | **Signing is done on your device. Your passkey's private key never goes to Vela.** (Synced passkeys are held by your password manager, end-to-end encrypted; security keys never export theirs.) | **签名在你的设备上完成。通行密钥的私钥绝不会交给 Vela。**（同步的通行密钥由你的密码管理器端到端加密保管；安全密钥从不导出私钥。） | passkeys doc | not "Vela never receives your passkey" — a passkey is never sent anywhere; the claim is about its private key (founder, 2026-09-22) |
| C-clear-2 | **What you see is what you sign:** the confirm screen is decoded from the exact transaction that is signed. Limits in C-clear-1 and C-selfcall-1; the independent signing page is not published yet, so do not call it a separate "signing channel". | **所见即所签：**确认页的内容就是从实际签名的那笔交易解码出来的。限制见 C-clear-1 与 C-selfcall-1；独立签名页尚未发布，不要称之为另一条“签名通道”。 | [§1]; bybit-attack "What Vela does about it" | positive framing (founder, 2026-09-22); the Bybit page keeps the limits |
| C-aud-1 | **Audience (founder, 2026-09-22):** Vela is written for people who self-host and build from source — the ones who will pay. Pages lead with running it yourself (apps, relay, index, chain data, rates), running your own relay, custom networks, and the one thing that can't move (the passkeys' domain). | **读者（创始人，2026-09-22）：**Vela 的文案写给会自己部署、自己编译的人——也就是会付费的人。页面先讲自己运行（App、中继、索引、链数据、汇率）、运行自己的中继、自定义网络，以及唯一换不掉的东西（通行密钥所属的域名）。 | founder | not a claim to print; a rule for ordering and emphasis |
| C-token-1 | No token, and no plans for one. | 没有代币，也没有发币计划。 | A02 | — |
| C-alpha-1 | Vela is alpha software: it works and holds real funds; start with small amounts. | Vela 仍是 alpha 阶段的软件：可以正常使用、承载真实资金；请先用小额。 | A02 FR-3 | — |

## Glossary (en ↔ zh, for translators)

| English | 中文 | Note |
|---|---|---|
| key (a signing credential) | 钥匙 | umbrella term; a passkey, a phone, a security key |
| passkey | 通行密钥 | per spec 059 terminology table |
| security key | 安全密钥 / 硬件安全密钥 | YubiKey-class |
| relay | 中继 | Vela's ERC-4337 bundler |
| relying party / rpId | 依赖方（RP ID） | |
| self-host | 自托管 / 自己部署 | |
| registry | 注册表 | the on-chain contract |
| public-key index | 公钥索引 | the service |
| signing page | 签名页 | `app-web/clearsigning` |
| clear signing | 清晰签名 | |
| blind signing | 盲签 | |
