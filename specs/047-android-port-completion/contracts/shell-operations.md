# Shell operations — 047
| Seam | Shell duty |
| --- | --- |
| `payment_request` `LinkOpened{to, chain, token, amount, sym, dec, net}` | from a deep link's query; `view.pay` drives `send.open(locked)` |
| `session` `sign_out_confirmed` | after an erase's verified sweep |
| `network_admin` probes (`OverrideExpanded`, `EndpointsOpened`) | already live; the pills read `rpc_health.latency_ms` |
| `rpc_pool` view `failed_chains` / `rate_limited_chains` | the RPC banner |
| documents `share(name, "image/png", bytes)` | the share card |
