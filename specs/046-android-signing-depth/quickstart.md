# Quickstart — verifying 046 on the device
1. Simulation: test dApp `send` → the sheet shows 「余额变化」 with `−0.001 XDAI` → slide → the feed row `−0.001 XDAI`.
2. Messages: test dApp `siwe` → 「登录」 + domain + binding OK; `siwe-bad` → mismatch warning; `ethsign` → the danger surface.
3. Scanner: push `qr-pay-gnosis.png` → 转账 → scan icon → 相册 → pick → Send locked on Gnosis, 0.001 XDAI to the founder; the camera preview opens/closes; push `qr-pay-arbitrum.png` → pick → the network is added (or 「未找到」).
