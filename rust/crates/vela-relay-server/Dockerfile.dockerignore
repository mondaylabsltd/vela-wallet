# Build context: rust/. Cargo needs every workspace member's manifest and
# sources to load the workspace, but not build output, generated bindings,
# test corpora, or the Worker (its own workspace).
**/target
pkg-web
harness
bindings
rust-toolchain.toml
crates/vela-relay-worker
crates/*/tests
**/.DS_Store
