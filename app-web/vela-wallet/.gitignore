node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# e2e artifacts
e2e/__screenshots__/

# Wrangler-generated types (recreated by check/build)
worker-configuration.d.ts

# The onboarding runtime wasm, copied from the repo's public/ by
# scripts/sync-wasm.mjs. Generated, not authored — committing a 3.4 MB binary
# twice is a 3.4 MB binary committed twice. `npm run check` gates staleness.
static/vela_core_bg.*.wasm

# spec 027: the packaged extension is build output
extension/dist
.pw-out*