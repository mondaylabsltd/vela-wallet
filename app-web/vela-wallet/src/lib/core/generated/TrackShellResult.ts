// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { TrackLifecycle } from "./TrackLifecycle";
import type { TrackPendingRecord } from "./TrackPendingRecord";
import type { TrustReceiptLog } from "./TrustReceiptLog";

/**
 * What the shell observed. Every time-bearing variant carries `now_ms`
 * (epoch milliseconds, f64) — the core is a pure function of its inputs.
 */
export type TrackShellResult = { "type": "clock", now_ms: number, } | { "type": "receipt", user_op_hash: string, tx_hash: string, now_ms: number, } | { "type": "receipt_with_logs", user_op_hash: string, tx_hash: string, now_ms: number, logs: Array<TrustReceiptLog>, } | { "type": "receipt_failed", user_op_hash: string, tx_hash: string, now_ms: number, } | { "type": "receipt_pending", user_op_hash: string, now_ms: number, } | { "type": "receipt_unreachable", user_op_hash: string, now_ms: number, } | { "type": "status", user_op_hash: string, status: TrackLifecycle, 
/**
 * Executor stage that last touched the op, e.g.
 * `in_band_settlement_hold`.
 */
stage: string | null, now_ms: number, } | { "type": "status_unavailable", user_op_hash: string, now_ms: number, } | { "type": "records_loaded", records: Array<TrackPendingRecord>, now_ms: number, } | { "type": "records_patched" } | { "type": "notified" };
