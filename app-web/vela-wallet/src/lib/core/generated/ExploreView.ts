// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ExploreGroupView } from "./ExploreGroupView";
import type { ExploreSite } from "./ExploreSite";
import type { ExploreTab } from "./ExploreTab";

export type ExploreView = { favorites: Array<ExploreSite>, groups: Array<ExploreGroupView>, tabs: Array<ExploreTab>, 
/**
 * Always a tab that exists, whenever there is one at all.
 */
selected_tab: string | null, favorites_hidden: boolean, recent_hidden: boolean, 
/**
 * The grid is full: the shell draws no "add" affordance rather than one
 * that refuses. A control that cannot work is worse than an absent one.
 */
favorites_full: boolean, 
/**
 * The strip is full, for the same reason.
 */
tabs_full: boolean, 
/**
 * The mirror is live. Before this, a screen shows nothing rather than an
 * empty start page it would have to correct a frame later.
 */
ready: boolean, };
